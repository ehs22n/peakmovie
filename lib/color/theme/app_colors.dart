import 'package:flutter/material.dart';

class AppColors {
  // DARK
  static const darkBackground = Color(0xFF0F1115);
  static const darkCard = Color(0xFF181B22);
  static const darkSurface = Color(0xFF222733);
  static const darkText = Colors.white;
  static const darkSubText = Color(0xFFA1A7B3);

  // LIGHT
  static const lightBackground = Color(0xFFF5F7FB);
  static const lightCard = Colors.white;
  static const lightSurface = Color(0xFFEAECEF);
  static const lightText = Color(0xFF111827);
  static const lightSubText = Color(0xFF6B7280);

  // BRAND COLORS
  static const primary = Color(0xFFE50914);
  static const secondary = Color(0xFF00C2FF);
}
